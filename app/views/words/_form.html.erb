<%= form_for(@word, :remote => (params[:action] == 'new' ? true : false)) do |f| %>
  <% if @word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@word.errors.count, "error") %> prohibited this word from being saved:</h2>

      <ul>
      <% @word.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="search">

    <%= f.text_field :word, :class => "search-form", :placeholder => 'Enter words to find definitions...', 
                                                                :autofocus => true, :required => true %>

    <%= f.submit :disable_with => 'Submitting...', :class => "search-glass", :src => "search-icon.png", :type => "image" %>

  </div>


<% end %>
